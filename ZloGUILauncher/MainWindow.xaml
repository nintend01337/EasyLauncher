<Controls:MetroWindow x:Class="ZloGUILauncher.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:local="clr-namespace:ZloGUILauncher"
                      xmlns:views="clr-namespace:ZloGUILauncher.Views"
                      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      xmlns:helpers="clr-namespace:ZloGUILauncher"
                      Height="600" Width="1000" ResizeMode="CanMinimize" ShowSystemMenuOnRightClick="false"
                      GlowBrush="{DynamicResource AccentColorBrush}" ShowTitleBar="True" ShowIconOnTitleBar="true" Icon="icon.ico"
                      WindowStartupLocation="CenterScreen">
    <Control.Background>
        <ImageBrush ImageSource="{StaticResource background}" Stretch="UniformToFill"/>
    </Control.Background>

    <FrameworkElement.Resources>



        <Style TargetType="{x:Type ScrollViewer}">
            <Style.Setters>
                <Setter Property="helpers:ScrollViewerCorrector.FixScrolling" Value="true"/>
            </Style.Setters>
        </Style>
        <Style TargetType="{x:Type Button}" BasedOn="{StaticResource MetroButton}">
            <Style.Setters>
                <Setter Property="Control.Background" Value="LightGray"/>
                <Setter Property="Control.Foreground" Value="Black"/>
            </Style.Setters>
        </Style>
    </FrameworkElement.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Rectangle Fill="{DynamicResource AccentColorBrush}" HorizontalAlignment="Left" Height="548.04" VerticalAlignment="Top" Width="110" Grid.RowSpan="2"/>
        <Controls:MetroAnimatedTabControl SelectionChanged="MainTabControl_SelectionChanged" x:Name="MainTabControl" Grid.Row="1"
                                      TabStripPlacement="Left" Controls:TabControlHelper.IsUnderlined="true" Background="#CC252525">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Grid Name="Panel">
                                    <ContentPresenter x:Name="ContentSite"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        ContentSource="Header"
                                        Margin="10,2"/>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Panel" Property="Background" Value="LightSkyBlue" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter TargetName="Panel" Property="Background" Value="{DynamicResource ResourceKey}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            <TabItem Margin="0,0,0.5,0" HorizontalAlignment="Center" HorizontalContentAlignment="Center" VerticalContentAlignment="Stretch" Height="112" Width="110">
                <TabItem.Header>
                    <Image Width="90" Source="media/256x256.png" MinWidth="80" MinHeight="90" Height="90"/>
                </TabItem.Header>
                <Grid>
                    <views:BF3View Visibility="Visible"/>
                </Grid>
            </TabItem>
            <TabItem Height="112" Width="110">
                <TabItem.Header>
                    <Image Width="90" Source="media/bf4.png" MinWidth="80" MinHeight="90" Height="90"/>
                </TabItem.Header>
                <Grid>
                    <views:BF4View x:Name="Ibf4View"/>
                </Grid>
            </TabItem>
            <TabItem HorizontalContentAlignment="Center" VerticalContentAlignment="Stretch" Height="112" Width="110">
                <TabItem.Header>
                    <Image Width="90" Source="media/bfh.png" MinWidth="80" MinHeight="90" Height="90" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0"/>
                </TabItem.Header>
                <Grid>
                    <views:BFHView x:Name="IbfhView"/>
                </Grid>
            </TabItem>
            <TabItem Header="Options" Margin="0,187.08,0.25,-192.08" HorizontalAlignment="Right" Width="55.684">
                <Grid>
                    <views:Options/>
                </Grid>
            </TabItem>
            <TabItem Header="Log" Margin="0,167.12,0,-172.12" HorizontalAlignment="Left" Width="53.413">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <RichTextBox x:Name="LogBox" VerticalScrollBarVisibility="Visible" BorderThickness="0" Background="#FF0D0D0D"
                       IsReadOnly="true" Grid.ColumnSpan="2">
                        <RichTextBox.Resources>
                            <Style TargetType="{x:Type Paragraph}">
                                <Setter Property="Margin" Value="0"/>
                            </Style>
                        </RichTextBox.Resources>
                        <FlowDocument x:Name="LogDocument"/>
                    </RichTextBox>
                    <Button Click="ClearButton_Click" Grid.Row="1" Content="Очистить"/>
                </Grid>
            </TabItem>
        </Controls:MetroAnimatedTabControl>
        <StatusBar Grid.Row="2">
            <TextBlock VerticalAlignment="Center" Text="Последнее событие : "/>
            <TextBlock x:Name="LatestGameStateTextBlock" VerticalAlignment="Center" Text="[Game] [type] state"/>
        </StatusBar>
        <TextBlock HorizontalAlignment="Right" VerticalAlignment="Bottom" Text="Developed by nintend01337"
               Margin="0,0,9,7" Grid.Row="2" Foreground="White"/>
    </Grid>
    <Controls:MetroWindow.RightWindowCommands>
        <Controls:WindowCommands>
            <StackPanel Orientation="Horizontal">
                <Image Source="Media/icon_reboot.png"/>
                <Button Click="RestartLauncherButton_Click" Content="Restart"/>
                <TextBlock Text="         "/>
                <Image Source="Media/discord_logo_white.PNG"/>
                <Button Click="OfficialDiscordButton_Click" Content="Discord"/>
            </StackPanel>
        </Controls:WindowCommands>
    </Controls:MetroWindow.RightWindowCommands>
</Controls:MetroWindow>